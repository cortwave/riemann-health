---
- name: Bring up docker containers
  hosts: localhost
  gather_facts: false
  vars:
    inventory:
      - name: health_host_1
        image: "chrismeyers/centos6"
  roles:
    - role: provision_docker
      provision_docker_inventory: "{{ inventory }}"

- name: Run riemann-health Tests
  hosts: docker_containers
  roles:
    - role: riemann-health
      riemann-server-host: "127.0.0.1"
  tasks:
    - name: Test
      command: 'echo "hello world"'
